<%= turbo_page_requires_reload %>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <%= render "rubits/rubit", rubit: @rubit %>
      <div class="mt-3">
        <%= render "form", parent_rubit: @rubit %>
      </div>
      <hr>
      <div id="rubits">
        <%= render partial: "rubit", collection: @rubit.child_rubits, as: :rubit %>
      </div>
    </div>
  </div>
</div>
