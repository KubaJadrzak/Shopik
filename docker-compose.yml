services:
  web:
    build: .
    env_file:
      - .env
    depends_on:
      - redis

  sidekiq:
    build: .
    command: bundle exec sidekiq
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:6
    volumes:
      - redisdata:/data

volumes:
  redisdata:
